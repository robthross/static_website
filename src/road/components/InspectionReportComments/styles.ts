import styled from 'styled-components'

import theme from '../../../common/styles/theme'

export const StyledComments = styled.div`
  border: 1px solid ${theme.colors.blue.blue50};
  padding: 2rem;
  width: 100%;
`
