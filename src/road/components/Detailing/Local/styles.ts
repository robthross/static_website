import styled from 'styled-components'

import theme from '../../../../common/styles/theme'

export const StyledLocal = styled.div`
  padding: 11px 14px;
  border: 1px dashed ${theme.colors.pure.pure100};
  border-radius: ${theme.border.radius10};
  margin-bottom: 24px;
`
